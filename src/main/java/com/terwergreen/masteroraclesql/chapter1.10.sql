-- 下订单超过4次的女顾客
SELECT c.CUSTOMER_ID,COUNT(o.ORDER_ID) as oracles_ct
  FROM OE.CUSTOMERS c
  JOIN OE.ORDERS o
  ON c.CUSTOMER_ID=o.CUSTOMER_ID
  GROUP BY c.CUSTOMER_ID
  HAVING COUNT(o.ORDER_ID) > 4
  ORDER BY oracles_ct, c.CUSTOMER_ID